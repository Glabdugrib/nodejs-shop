<% if(products.length> 0) { %>
   <div class="row row-cards row-deck g-4 mt-5">
      <% for(const product of products) { %>
         <div class="col-6 col-md-4 col-lg-3">

            <div class="product-card card text-center border rounded-4 overflow-hidden">
               <!-- CONTINUARE DA QUI -->
               <img class="card-img-top img-fluid bg-image hover-zoom"
                  src="<%= product.imageUrl %>" alt="<%= product.title %>">
               <div class="card-header justify-content-center">
                  <h5 class="card-title text-truncate">
                     <%= product.title %>
                  </h5>
               </div>
               <div class="card-body">
                  <p class="card-description card-text overflow-hidden"><%= product.description %></p>
               </div>
               <div class="card-footer card-footer-transparent">
                  <p class="card-text fs-3"><%= product.price %>&euro;</p>
               </div>
               <div class="card-footer d-flex justify-content-center g-2">
                  <!-- TODO: aggiungere rotta dinamica -->
                  <a href="/products/<%= product.id %>" class="btn btn-icon btn-outline-indigo btn-pill p-2 me-3" title="View Details">
                     <i class="ti ti-eye"></i>
                  </a>
                  <!-- <a class="btn btn-icon btn-outline-teal btn-pill p-2 me-3" title="Add to Cart">
                     <i class="ti ti-plus"></i>
                  </a> -->
                  <form action="/cart" method="post">
                     <input type="hidden" name="productId" value="<%= product.id %>">
                     <button class="btn btn-icon btn-outline-teal btn-pill p-2" type="submit" title="Add to Cart">
                        <i class="ti ti-plus"></i>
                     </button>
                  </form>
               </div>
            </div>
         </div>
         <% } %>
   </div>
<% } else { %>
   <div class="mt-5">
      <p class="text-danger">No Products found!</p>
   </div>
<% } %>